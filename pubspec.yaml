name: dio_logger_plus
description: "A new Flutter project."
version: 1.0.0
homepage: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ">=5.7.0 <6.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter